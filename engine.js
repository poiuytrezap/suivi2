/* engine.js — GENERE PAR build.py, NE PAS EDITER A LA MAIN.
 *
 * Le moteur Python embarque. Aucun fetch, donc aucun chemin relatif
 * a se tromper et aucune page 404 qui finirait interpretee comme du
 * Python. Regenere avec : python build.py
 */
window.ENGINE = {
  modules: ["stats", "energie", "perfs", "bridge"],
  sha: {
    "stats": "388795ad8c7cee142fadb73d591c4be7eb6ba3cc1d98aebcc969e8b2794f47f2",
    "energie": "17ef91eba66406588db88dd3cf511460df688d69d2c7d1ade4fa8c2db0b48f93",
    "perfs": "66203aa2bc80f5f6cfb92da8ee9a453444895ade8e3eae86ca2186a149d115f8",
    "bridge": "9fb6047da74bb0d6f6e561d3720a5f08c632c5d47a6379afaa950088734e1283"
},
  src: {
    stats: "# -*- coding: utf-8 -*-\n\"\"\"\nStatistiques en stdlib pure, en remplacement de numpy dans le moteur.\n\nPourquoi\n--------\nLe moteur (energie.py, perfs.py) n'utilisait numpy que pour 22 appels, tous\nelementaires : moyenne, ecart-type, somme, une regression lineaire de degre 1\net un coefficient de Pearson. Rien qui justifie une dependance de 15 Mo.\n\nL'enjeu n'est pas la place sur disque, c'est l'iPhone. Sans serveur, le moteur\ndoit tourner dans Safari via Pyodide (Python compile en WebAssembly). Avec\nnumpy : ~15 Mo et 5-10 s de demarrage a froid. Sans : ~6 Mo et 1-2 s.\n\nPoint crucial : ce n'est PAS une seconde implementation. energie.py et\nperfs.py restent des fichiers uniques, partages a l'identique entre\nl'application Mac et l'application iPhone. Reecrire la logique en JavaScript\naurait cree deux implementations qui divergent ; remplacer numpy par de la\nstdlib dans le meme fichier n'en cree aucune.\n\nChaque fonction reproduit la semantique numpy exacte utilisee par le moteur,\ny compris le ddof (degres de liberte) qui n'est pas le meme partout :\nnumpy.std utilise ddof=0 par defaut, et le moteur appelle explicitement\nddof=1 pour l'ecart-type residuel. Se tromper la fausse les intervalles de\nconfiance.\n\"\"\"\n\nfrom math import isnan, sqrt\n\n\ndef mean(v):\n    v = list(v)\n    return sum(v) / len(v) if v else float(\"nan\")\n\n\ndef _sum_sq_dev(v, m):\n    return sum((x - m) ** 2 for x in v)\n\n\ndef std(v, ddof=0):\n    \"\"\"Ecart-type. ddof=0 = numpy par defaut ; ddof=1 = estimateur non biaise.\"\"\"\n    v = list(v)\n    n = len(v)\n    if n - ddof <= 0:\n        return float(\"nan\")\n    return sqrt(_sum_sq_dev(v, mean(v)) / (n - ddof))\n\n\ndef ptp(v):\n    v = list(v)\n    return (max(v) - min(v)) if v else 0.0\n\n\ndef polyfit1(x, y):\n    \"\"\"Regression lineaire de degre 1 par moindres carres.\n\n    Equivalent de numpy.polyfit(x, y, 1), qui retourne [pente, ordonnee].\n    Formule fermee : pente = Sxy / Sxx, ordonnee = ybar - pente * xbar.\n    \"\"\"\n    x, y = list(map(float, x)), list(map(float, y))\n    n = len(x)\n    if n < 2:\n        return float(\"nan\"), float(\"nan\")\n    mx, my = mean(x), mean(y)\n    sxx = _sum_sq_dev(x, mx)\n    if sxx == 0:\n        return float(\"nan\"), float(\"nan\")\n    sxy = sum((x[i] - mx) * (y[i] - my) for i in range(n))\n    pente = sxy / sxx\n    return pente, my - pente * mx\n\n\ndef corrcoef(x, y):\n    \"\"\"Coefficient de correlation de Pearson (numpy.corrcoef(x, y)[0, 1]).\"\"\"\n    x, y = list(map(float, x)), list(map(float, y))\n    n = len(x)\n    if n < 2:\n        return float(\"nan\")\n    mx, my = mean(x), mean(y)\n    sxx, syy = _sum_sq_dev(x, mx), _sum_sq_dev(y, my)\n    if sxx == 0 or syy == 0:\n        return float(\"nan\")\n    sxy = sum((x[i] - mx) * (y[i] - my) for i in range(n))\n    return sxy / sqrt(sxx * syy)\n\n\ndef rolling_mean(values, window=7):\n    \"\"\"Moyenne glissante arriere, tolerante aux debuts de serie.\n\n    Identique a l'implementation numpy d'origine : pour i < window, la moyenne\n    porte sur les i+1 premieres valeurs plutot que de produire un NaN.\n    \"\"\"\n    values = list(values)\n    return [mean(values[max(0, i - window + 1):i + 1]) for i in range(len(values))]\n",
    energie: "# -*- coding: utf-8 -*-\n\"\"\"\nModele de depense energetique pour Suivi Coach.\n\nPhilosophie du module\n---------------------\nDeux estimateurs cohabitent, et c'est volontaire :\n\n1. BILAN ENERGETIQUE (reference, non biaisee)\n       TDEE = apport moyen - (pente de poids en kg/j) * 7700\n   C'est la seule mesure vraiment \"vraie\" : elle ne suppose aucun modele\n   physiologique. Sa faiblesse : elle donne UNE moyenne sur une fenetre, elle\n   est bruitee (la pente est incertaine), et elle ne dit rien du jour a jour.\n\n2. MODELE PAR COMPOSANTES (explicatif, biaise)\n       TDEE_j = BMR + TEF_j + cout_pas_j + cout_seance_j + cout_cardio_j\n   Il predit chaque jour, il explique pourquoi un jour a 25000 pas differe\n   d'un jour a 12000. Sa faiblesse : c'est un empilement d'estimations dont\n   les erreurs s'additionnent (Mifflin +/-10 %, cout/pas +/-30 %...).\n\nLa calibration reconcilie les deux : on ajuste le modele par composantes pour\nque sa moyenne colle au bilan energetique observe. On garde alors la precision\ndu bilan ET la granularite du modele.\n\"\"\"\n\nfrom datetime import date, timedelta\nfrom math import sqrt\n\nimport stats as S\n\nKCAL_PAR_KG = 7700.0\n\n# METs nets par defaut (valeurs du Compendium of Physical Activities, ajustees\n# a la baisse pour la musculation : le compendium surestime notoirement le\n# cout reel d'une seance avec des temps de repos longs).\nMETS_DEFAUT = {\n    \"Push A\": 4.0,\n    \"Pull\": 4.0,\n    \"Push B\": 4.0,\n    \"Lower\": 4.5,   # jambes : plus de masse musculaire recrutee\n    \"Upper\": 4.0,\n    \"Full Body\": 4.5,\n    \"Repos\": 0.0,\n}\n\nPROFIL_DEFAUT = {\n    \"sexe\": \"H\",\n    \"taille_cm\": 178.0,\n    \"age\": 30.0,\n    \"bodyfat_pct\": 0.0,          # 0 => Mifflin-St Jeor, sinon Katch-McArdle\n    \"seance_min_defaut\": 75.0,\n    \"cardio_met\": 6.0,\n    \"kcal_par_pas_par_kg\": 0.00045,   # cout NET de la marche\n    \"tef_prot\": 0.25,\n    \"tef_gluc\": 0.08,\n    \"tef_lip\": 0.02,\n    \"k_calib\": 1.0,              # facteur de calibration global\n    \"k_calib_date\": \"\",\n}\n\n\n# ---------------------------------------------------------------------------\n# Composantes\n# ---------------------------------------------------------------------------\n\ndef bmr(poids_kg: float, profil: dict) -> float:\n    \"\"\"Metabolisme de base.\n\n    Katch-McArdle si le % de masse grasse est renseigne (nettement meilleur\n    chez un pratiquant avec une composition corporelle atypique : Mifflin ne\n    voit que le poids total et sous-estime un physique tres seche et muscle).\n    Sinon Mifflin-St Jeor.\n    \"\"\"\n    bf = float(profil.get(\"bodyfat_pct\") or 0.0)\n    if bf > 0:\n        lbm = poids_kg * (1.0 - bf / 100.0)\n        return 370.0 + 21.6 * lbm\n    taille = float(profil.get(\"taille_cm\", 178.0))\n    age = float(profil.get(\"age\", 30.0))\n    base = 10.0 * poids_kg + 6.25 * taille - 5.0 * age\n    return base + (5.0 if profil.get(\"sexe\", \"H\") == \"H\" else -161.0)\n\n\ndef tef(prot_g, gluc_g, lip_g, profil: dict) -> float:\n    \"\"\"Effet thermique des aliments, calcule par macro plutot qu'a 10 % forfait.\n\n    C'est un gain reel ici : a 151 g de proteines, le TEF est sensiblement\n    plus eleve que le forfait 10 % que tout le monde utilise.\n    \"\"\"\n    if prot_g is None or gluc_g is None or lip_g is None:\n        return 0.0\n    return (profil.get(\"tef_prot\", 0.25) * 4.0 * float(prot_g)\n            + profil.get(\"tef_gluc\", 0.08) * 4.0 * float(gluc_g)\n            + profil.get(\"tef_lip\", 0.02) * 9.0 * float(lip_g))\n\n\ndef cout_pas(pas, poids_kg: float, profil: dict) -> float:\n    \"\"\"Cout net de la marche. Il SCALE avec le poids : c'est exactement le\n    point souleve - 16000 pas a 73 kg ne coutent pas la meme chose qu'a 68 kg.\n    \"\"\"\n    if not pas:\n        return 0.0\n    return float(pas) * poids_kg * float(profil.get(\"kcal_par_pas_par_kg\", 0.00045))\n\n\ndef cout_seance(nom_seance, minutes, poids_kg: float, profil: dict) -> float:\n    \"\"\"Cout net d'une seance : (MET - 1) * kg * heures.\n\n    Le \"-1\" retire la depense de repos, deja comptee dans le BMR : sans ca on\n    compte deux fois le metabolisme basal pendant l'heure d'entrainement.\n    \"\"\"\n    if not nom_seance:\n        return 0.0\n    mets = dict(METS_DEFAUT)\n    mets.update(profil.get(\"mets\", {}))\n    met = float(mets.get(nom_seance, 4.0))\n    if met <= 1.0:\n        return 0.0\n    mins = float(minutes) if minutes else float(profil.get(\"seance_min_defaut\", 75.0))\n    return (met - 1.0) * poids_kg * (mins / 60.0)\n\n\ndef cout_cardio(minutes, poids_kg: float, profil: dict) -> float:\n    if not minutes:\n        return 0.0\n    met = float(profil.get(\"cardio_met\", 6.0))\n    return max(0.0, met - 1.0) * poids_kg * (float(minutes) / 60.0)\n\n\ndef composantes(entry: dict, poids_kg: float, profil: dict) -> dict:\n    \"\"\"Decompose la depense d'une journee. Retourne un dict de kcal.\"\"\"\n    c = {\n        \"bmr\": bmr(poids_kg, profil),\n        \"tef\": tef(entry.get(\"proteines\"), entry.get(\"glucides\"),\n                   entry.get(\"lipides\"), profil),\n        \"pas\": cout_pas(entry.get(\"pas\"), poids_kg, profil),\n        \"seance\": cout_seance(entry.get(\"seance\"), entry.get(\"seance_min\"),\n                              poids_kg, profil),\n        \"cardio\": cout_cardio(entry.get(\"cardio\"), poids_kg, profil),\n    }\n    c[\"brut\"] = sum(c.values())\n    c[\"total\"] = c[\"brut\"] * float(profil.get(\"k_calib\", 1.0))\n    return c\n\n\n# ---------------------------------------------------------------------------\n# Bilan energetique (reference)\n# ---------------------------------------------------------------------------\n\ndef regression_poids(dates, poids):\n    \"\"\"OLS sur les pesees BRUTES.\n\n    Volontairement pas sur la moyenne glissante : le lissage introduit de\n    l'autocorrelation qui divise artificiellement l'erreur standard par ~2-3.\n    La pente attendue est la meme, mais l'incertitude calculee ici est honnete.\n    Retourne (pente kg/j, ordonnee, erreur standard de la pente, n).\n    \"\"\"\n    if len(poids) < 4:\n        return None\n    x = [float((d - dates[0]).days) for d in dates]\n    y = [float(v) for v in poids]\n    n = len(x)\n    mx = S.mean(x)\n    sxx = sum((v - mx) ** 2 for v in x)\n    if sxx == 0:\n        return None\n    pente, ordonnee = S.polyfit1(x, y)\n    resid = [y[i] - (pente * x[i] + ordonnee) for i in range(n)]\n    s2 = sum(r ** 2 for r in resid) / (n - 2)\n    se = sqrt(s2 / sxx)\n    return float(pente), float(ordonnee), se, n\n\n\ndef tdee_bilan(dates, poids, calories):\n    \"\"\"TDEE de reference sur une fenetre, avec intervalle de confiance 95 %.\n\n    L'IC vient entierement de l'incertitude sur la pente : c'est la, et pas\n    ailleurs, que se joue la fiabilite du chiffre.\n    \"\"\"\n    reg = regression_poids(dates, poids)\n    if reg is None or not calories:\n        return None\n    pente, _, se, n = reg\n    apport = float(S.mean(calories))\n    tdee = apport - pente * KCAL_PAR_KG\n    demi = 1.96 * se * KCAL_PAR_KG\n    return {\n        \"tdee\": tdee,\n        \"bas\": tdee - demi,\n        \"haut\": tdee + demi,\n        \"demi_ic\": demi,\n        \"apport_moyen\": apport,\n        \"pente_kg_j\": pente,\n        \"pente_kg_sem\": pente * 7.0,\n        \"se_pente\": se,\n        \"n_pesees\": n,\n        \"n_jours_kcal\": len(calories),\n    }\n\n\n# ---------------------------------------------------------------------------\n# Calibration\n# ---------------------------------------------------------------------------\n\ndef calibrer(jours, profil: dict):\n    \"\"\"Ajuste un facteur global k pour que le modele colle au bilan.\n\n    jours : liste de dicts {date, entry, poids_lisse}\n\n    Un SEUL parametre libre, et c'est un choix assume : avec 4 a 8 semaines de\n    donnees on ne peut pas identifier separement BMR, cout/pas et cout/seance.\n    Ils sont trop correles (plus de pas les jours de seance, poids qui derive\n    lentement). Vouloir les fitter tous revient a surajuster du bruit.\n    \"\"\"\n    dates = [j[\"date\"] for j in jours if j.get(\"poids_brut\") is not None]\n    poids = [j[\"poids_brut\"] for j in jours if j.get(\"poids_brut\") is not None]\n    kcal = [j[\"entry\"][\"calories\"] for j in jours\n            if isinstance(j[\"entry\"].get(\"calories\"), (int, float))]\n    if len(dates) < 8 or len(kcal) < 8:\n        return None\n\n    ref = tdee_bilan(dates, poids, kcal)\n    if ref is None:\n        return None\n\n    p_neutre = dict(profil)\n    p_neutre[\"k_calib\"] = 1.0\n    bruts = [composantes(j[\"entry\"], j[\"poids_lisse\"], p_neutre)[\"brut\"]\n             for j in jours if j.get(\"poids_lisse\")]\n    if not bruts:\n        return None\n    modele_moyen = float(S.mean(bruts))\n    if modele_moyen <= 0:\n        return None\n\n    k = ref[\"tdee\"] / modele_moyen\n    ref[\"k\"] = k\n    ref[\"k_bas\"] = ref[\"bas\"] / modele_moyen\n    ref[\"k_haut\"] = ref[\"haut\"] / modele_moyen\n    ref[\"modele_moyen\"] = modele_moyen\n    ref[\"ecart_kcal\"] = ref[\"tdee\"] - modele_moyen\n    return ref\n\n\ndef plausibilite_k(k: float):\n    \"\"\"Un k hors plage physiologique n'est PAS une info sur le metabolisme.\n\n    C'est un signal que les donnees d'entree sont incoherentes. Le modele par\n    composantes se trompe rarement de plus de ~25 % : un k de 0.6 ou de 1.5\n    signifie que l'apport declare, la pesee ou la fenetre posent probleme, pas\n    que le sujet a un metabolisme hors norme. Refuser de calibrer dans ce cas\n    est plus utile que d'enregistrer un chiffre faux avec 3 decimales.\n    \"\"\"\n    if 0.80 <= k <= 1.25:\n        return True, \"\"\n    causes = []\n    if k < 0.80:\n        causes = [\n            \"poids qui monte trop vite pour l'apport declare (retention \"\n            \"hydrique, reprise de glycogene, ou apport reel superieur au \"\n            \"declare)\",\n            \"fenetre trop courte : une derive de pesee de quelques centaines \"\n            \"de grammes suffit a fausser la pente\",\n            \"balance ou conditions de pesee non constantes\",\n        ]\n    else:\n        causes = [\n            \"poids qui stagne ou baisse malgre un apport eleve (sous-\"\n            \"declaration classique, souvent 15-25 % sur les hors-repas)\",\n            \"pas ou seances largement surestimes\",\n            \"fenetre trop courte pour que la pente ait un sens\",\n        ]\n    return False, (\"k = %.2f sort de la plage physiologiquement credible \"\n                   \"(0.80 - 1.25). Ce n'est pas une caracteristique de ton \"\n                   \"metabolisme, c'est un defaut de coherence des donnees. \"\n                   \"Causes probables :\\n  - %s\" % (k, \"\\n  - \".join(causes)))\n\n\ndef diagnostic_k(k: float) -> str:\n    if k >= 1.12:\n        return (\"Le modele SOUS-estime. Causes probables : NEAT eleve non \"\n                \"capture par le podometre (station debout, agitation), \"\n                \"sous-declaration des apports, ou masse maigre superieure a \"\n                \"ce que suppose la formule.\")\n    if k <= 0.88:\n        return (\"Le modele SUR-estime. Causes probables : double comptage \"\n                \"(les pas de la seance sont comptes DANS le nombre de pas ET \"\n                \"dans le cout de la seance), METs de musculation trop \"\n                \"genereux, ou podometre optimiste.\")\n    return (\"Modele et bilan energetique concordent. Les coefficients par \"\n            \"defaut sont adaptes a ton cas.\")\n\n\ndef kcal_cible(tdee: float, poids_kg: float, pct_par_semaine: float) -> float:\n    \"\"\"Apport a viser pour une vitesse de prise/perte en %/semaine.\n\n    En lean bulk, le %/semaine est la bonne unite : 0,25 %/sem a 73 kg n'est\n    pas la meme chose qu'a 90 kg.\n    \"\"\"\n    delta_kg_sem = poids_kg * pct_par_semaine / 100.0\n    return tdee + (delta_kg_sem * KCAL_PAR_KG) / 7.0\n",
    perfs: "# -*- coding: utf-8 -*-\n\"\"\"\nAnalyse des performances pour Suivi Coach.\n\nTrois pieges que ce module traite explicitement\n-----------------------------------------------\n1. UNITE D'ANALYSE = (seance, exercice), jamais l'exercice seul.\n   TIRAGE UNILATERAL est programme en PULL (gamme 5/8, lourd) ET en UPPER\n   (gamme 8/10, plus leger). Les fusionner fabrique un zigzag que la\n   regression lit comme une tendance : +44 % de progression qui n'existe pas.\n   Meme exercice, meme materiel, mais programmation et fatigue accumulee\n   differentes : ce sont deux series temporelles distinctes.\n\n2. LES BAISSES DE CHARGE NE SONT PAS DES PERTES DE FORCE.\n   Une correction technique fait chuter la charge sans qu'un gramme de muscle\n   ne soit perdu. Dans ce jeu de donnees, TOUTES les elevations laterales\n   chutent en semaine 4-5, exactement ou le coach a note \"Correction\n   feed-back video\". Annoncer \"-33 % de force\" serait faux et pousserait a\n   des conclusions inverses de la realite. On detecte ces ruptures et on\n   segmente la serie au lieu de tracer une droite a travers.\n\n3. e1RM (Epley) = charge * (1 + reps/30).\n   Au-dela de ~12 reps l'erreur depasse 10 % : sur une elevation laterale a\n   16 reps le chiffre n'a aucun sens ABSOLU. Il reste un indice de\n   progression valable tant qu'on ne compare que la meme serie du meme\n   exercice de la meme seance d'une semaine a l'autre.\n\"\"\"\n\nfrom datetime import date\nfrom math import atanh, ceil, isnan, sqrt\n\nimport stats as S\n\nSEUIL_RUPTURE = 0.12       # baisse de charge consideree comme non physiologique\n\n\ndef e1rm(charge, reps):\n    if not charge or not reps or reps <= 0:\n        return None\n    return float(charge) * (1.0 + float(reps) / 30.0)\n\n\ndef fiabilite_e1rm(reps):\n    if reps is None:\n        return \"\u2014\"\n    if reps <= 6:\n        return \"bonne\"\n    if reps <= 12:\n        return \"moyenne\"\n    return \"indicative\"\n\n\ndef seances_valides(seances):\n    return [s for s in seances if not s.get(\"exclu\")]\n\n\ndef par_exercice(seances):\n    \"\"\"{(seance, exercice): [points tries par date]} -- cle composite.\"\"\"\n    out = {}\n    for s in seances_valides(seances):\n        d = date.fromisoformat(s[\"date\"])\n        for ex in s[\"exercices\"]:\n            vals, vol, reps_l, charges = [], 0.0, [], []\n            for st in ex[\"sets\"]:\n                r, c = st.get(\"reps\"), st.get(\"charge\")\n                v = e1rm(c, r)\n                if v:\n                    vals.append(v)\n                    vol += float(r) * float(c)\n                    reps_l.append(r)\n                    charges.append(float(c))\n            if not vals:\n                continue\n            out.setdefault((s[\"seance\"], ex[\"nom\"]), []).append({\n                \"date\": d, \"semaine\": s[\"semaine\"], \"e1rm_max\": max(vals),\n                \"volume\": vol, \"muscle\": ex.get(\"muscle\"),\n                \"reps_max\": max(reps_l), \"charge_max\": max(charges),\n                \"n_sets\": len(vals), \"notes\": ex.get(\"notes\", []),\n            })\n    for k in out:\n        out[k].sort(key=lambda x: x[\"date\"])\n    return out\n\n\ndef detecter_ruptures(points):\n    \"\"\"Indices ou la charge chute anormalement -> changement de reference.\n\n    Une baisse de charge >12 % d'une semaine a l'autre chez un pratiquant en\n    surplus calorique n'est physiologiquement pas une perte de force. C'est\n    presque toujours : execution corrigee, machine differente, ou tentative\n    ratee. Une note du classeur sur le point abaisse le seuil a 5 %, car elle\n    confirme qu'il s'est passe quelque chose.\n    \"\"\"\n    ruptures = []\n    for i in range(1, len(points)):\n        av, ap = points[i - 1][\"charge_max\"], points[i][\"charge_max\"]\n        if av <= 0:\n            continue\n        baisse = (av - ap) / av\n        note = bool(points[i][\"notes\"]) or bool(points[i - 1][\"notes\"])\n        if baisse >= SEUIL_RUPTURE or (baisse >= 0.05 and note):\n            ruptures.append({\n                \"i\": i, \"date\": points[i][\"date\"], \"semaine\": points[i][\"semaine\"],\n                \"avant\": av, \"apres\": ap, \"baisse_pct\": baisse * 100.0,\n                \"notes\": points[i][\"notes\"] or points[i - 1][\"notes\"],\n            })\n    return ruptures\n\n\ndef segments(points):\n    \"\"\"Decoupe la serie aux ruptures. Une droite ne traverse pas une rupture.\"\"\"\n    idx = [r[\"i\"] for r in detecter_ruptures(points)]\n    bornes = [0] + idx + [len(points)]\n    return [points[bornes[i]:bornes[i + 1]] for i in range(len(bornes) - 1)\n            if bornes[i + 1] - bornes[i] > 0]\n\n\ndef tendance(points):\n    if len(points) < 3:\n        return None\n    x = [float((p[\"date\"] - points[0][\"date\"]).days) for p in points]\n    y = [float(p[\"e1rm_max\"]) for p in points]\n    if S.ptp(x) == 0 or y[0] == 0:\n        return None\n    pente, ordo = S.polyfit1(x, y)\n    resid = [y[i] - (pente * x[i] + ordo) for i in range(len(x))]\n    my = S.mean(y)\n    sst = sum((v - my) ** 2 for v in y)\n    r2 = 1 - sum(r ** 2 for r in resid) / sst if sst > 0 else float(\"nan\")\n    return {\"pente_sem\": float(pente) * 7.0,\n            \"pct_sem\": float(pente) * 7.0 / float(y[0]) * 100.0,\n            \"r2\": r2, \"n\": len(points), \"depart\": float(y[0]),\n            \"actuel\": float(y[-1]),\n            \"delta_pct\": (float(y[-1]) - float(y[0])) / float(y[0]) * 100.0}\n\n\ndef tendance_propre(points):\n    \"\"\"Tendance sur le DERNIER segment (apres la derniere rupture).\"\"\"\n    segs = segments(points)\n    if not segs:\n        return None, 0\n    dernier = segs[-1]\n    return tendance(dernier), len(segs) - 1\n\n\ndef prediction(points, semaines=8):\n    t, n_rup = tendance_propre(points)\n    if not t:\n        return None\n    return {**t, \"horizon_sem\": semaines,\n            \"projection\": t[\"actuel\"] + t[\"pente_sem\"] * semaines,\n            \"n_ruptures\": n_rup}\n\n\ndef indice_force(seances, ignorer_ruptures=True):\n    \"\"\"Indice global base 100 a la 1re seance de chaque (seance, exercice).\n\n    Chaque serie est normalisee par sa PROPRE base avant moyenne : sinon\n    l'indice ne refleterait que la presse a cuisse et le developpe.\n    ignorer_ruptures=True ecarte les series ou une correction technique a\n    change la reference -- sinon l'indice mesure le changement de forme, pas\n    la force.\n    \"\"\"\n    ex = par_exercice(seances)\n    par_sem, ecartes = {}, []\n    for cle, pts in ex.items():\n        if ignorer_ruptures and detecter_ruptures(pts):\n            ecartes.append(cle)\n            continue\n        base = pts[0][\"e1rm_max\"]\n        if not base:\n            continue\n        for p in pts:\n            par_sem.setdefault(p[\"semaine\"], []).append(p[\"e1rm_max\"] / base * 100.0)\n    ind = {s: float(S.mean(v)) for s, v in sorted(par_sem.items()) if len(v) >= 3}\n    return ind, ecartes\n\n\ndef volume_series_par_muscle(seances):\n    out = {}\n    for s in seances_valides(seances):\n        for ex in s[\"exercices\"]:\n            m = ex.get(\"muscle\") or \"?\"\n            n = sum(1 for st in ex[\"sets\"] if st.get(\"reps\") and st.get(\"charge\"))\n            out.setdefault(m, {})\n            out[m][s[\"semaine\"]] = out[m].get(s[\"semaine\"], 0) + n\n    return out\n\n\n# ---------------------------------------------------------------------------\n# Correlation force <-> masse\n# ---------------------------------------------------------------------------\n\ndef n_pour_detecter(r_cible=0.6):\n    z = atanh(min(abs(r_cible), 0.999))\n    return int(ceil(((1.96 + 0.84) / z) ** 2 + 3)) if z > 0 else 999\n\n\ndef correlation_force_masse(seances, poids_par_semaine):\n    \"\"\"Indice de force hebdo vs poids hebdo.\n\n    Ce que ca PEUT montrer : si force et poids evoluent ensemble.\n    Ce que ca NE PEUT PAS montrer : si la prise est du MUSCLE. Le poids =\n    muscle + gras + eau + contenu digestif. Une correlation force/poids reste\n    un indice faible. Sans tour de taille (absent du classeur), la part de\n    gras est invisible.\n    \"\"\"\n    ind, ecartes = indice_force(seances)\n    sems = [s for s in sorted(ind) if s in poids_par_semaine]\n    if len(sems) < 3:\n        return {\"n\": len(sems), \"verdict\": \"insuffisant\", \"ecartes\": ecartes}\n\n    x = [float(ind[s]) for s in sems]\n    y = [float(poids_par_semaine[s]) for s in sems]\n    n = len(sems)\n    r = (float(S.corrcoef(x, y))\n         if S.std(x) > 0 and S.std(y) > 0 else float(\"nan\"))\n\n    t_crit = {3: 12.706, 4: 4.303, 5: 3.182, 6: 2.776, 7: 2.571, 8: 2.447,\n              9: 2.365, 10: 2.306, 11: 2.262, 12: 2.228}.get(n, 2.0)\n    ddl = n - 2\n    r_requis = t_crit / sqrt(t_crit ** 2 + ddl) if ddl > 0 else 1.0\n    signif = (not isnan(r)) and abs(r) >= r_requis\n    return {\"n\": n, \"r\": r, \"r_requis\": r_requis, \"significatif\": signif,\n            \"semaines\": sems, \"force\": list(x), \"poids\": list(y),\n            \"ecartes\": ecartes, \"n_requis\": n_pour_detecter(0.6),\n            \"verdict\": \"significatif\" if signif else \"non concluant\"}\n\n\ndef diagnostic_correlation(res):\n    if res.get(\"verdict\") == \"insuffisant\":\n        return \"Moins de 3 semaines exploitables : rien a analyser.\"\n    n, r = res[\"n\"], res.get(\"r\")\n    if not res[\"significatif\"]:\n        return (\n            f\"AUCUNE CONCLUSION POSSIBLE \u2014 et c'est le resultat honnete.\\n\\n\"\n            f\"Avec n={n} semaines il faudrait |r| > {res['r_requis']:.2f} pour \"\n            f\"atteindre p<0.05 ; on observe r={r:+.2f}. Un coefficient calcule \"\n            f\"sur {n} points est domine par le bruit : il changera de valeur, \"\n            f\"et possiblement de signe, des la semaine prochaine. Il faut \"\n            f\"~{res['n_requis']} semaines pour detecter de facon fiable une \"\n            f\"correlation moderee.\\n\\n\"\n            f\"Et meme a {res['n_requis']} semaines : une correlation \"\n            f\"force/poids ne prouverait PAS un gain de muscle. Le poids inclut \"\n            f\"le gras et l'eau. Sans tour de taille, la question \"\n            f\"\\\"est-ce du muscle ?\\\" reste sans reponse.\")\n    return (f\"r={r:+.2f} sur {n} semaines (seuil |r|>{res['r_requis']:.2f}). \"\n            f\"Force et poids evoluent ensemble. Cela reste un indice faible de \"\n            f\"prise musculaire, pas une mesure : sans tour de taille, la part \"\n            f\"de gras est invisible.\")\n",
    bridge: "# -*- coding: utf-8 -*-\n\"\"\"\nAdaptateur entre le JSON du navigateur et le moteur d'analyse.\n\nCette couche ne contient AUCUNE logique metier : elle deserialise, appelle\nenergie.py / perfs.py a l'identique, et resserialise. Toute la reflexion est\ndans le moteur, partage tel quel avec l'application Mac.\n\"\"\"\n\nimport json\nimport math\nfrom datetime import date, timedelta\n\nimport energie as E\nimport perfs as P\nimport stats as S\n\nJOURS = [\"Lundi\", \"Mardi\", \"Mercredi\", \"Jeudi\", \"Vendredi\", \"Samedi\", \"Dimanche\"]\n\n\ndef _propre(o):\n    \"\"\"Remplace NaN et Infini par None avant serialisation.\n\n    json.dumps de Python emet NaN / Infinity, qui ne sont PAS du JSON valide :\n    JSON.parse du navigateur les rejette et fait tomber toute l'analyse. Le\n    cas se produit reellement ici \u2014 le R2 d'une serie parfaitement plate vaut\n    NaN (variance totale nulle). Python etant laxiste et le navigateur strict,\n    ce bug ne se voit qu'a l'integration.\n    \"\"\"\n    if isinstance(o, float):\n        return None if (math.isnan(o) or math.isinf(o)) else o\n    if isinstance(o, dict):\n        return {k: _propre(v) for k, v in o.items()}\n    if isinstance(o, (list, tuple)):\n        return [_propre(v) for v in o]\n    return o\n\n\ndef lundi(d):\n    return d - timedelta(days=d.weekday())\n\n\ndef _series(entries, cle):\n    pts = sorted((date.fromisoformat(k), float(v[cle]))\n                 for k, v in entries.items()\n                 if isinstance(v.get(cle), (int, float)))\n    return [a for a, b in pts], [b for a, b in pts]\n\n\ndef _jours(entries, nb=None):\n    ds, vs = _series(entries, \"poids\")\n    if not vs:\n        return []\n    lis = S.rolling_mean(vs, 7)\n    par_date = {d: (v, l) for d, v, l in zip(ds, vs, lis)}\n    toutes = sorted(date.fromisoformat(i) for i in entries)\n    if nb:\n        toutes = [d for d in toutes if d >= toutes[-1] - timedelta(days=nb - 1)]\n    out, last = [], None\n    for d in toutes:\n        brut, l = par_date.get(d, (None, None))\n        if l is not None:\n            last = l\n        if last is None:\n            continue\n        out.append({\"date\": d, \"entry\": entries[d.isoformat()],\n                    \"poids_brut\": brut, \"poids_lisse\": last})\n    return out\n\n\ndef _semaines_requises(sigma, ecart_kg_sem, n_actuel):\n    \"\"\"Combien de semaines avant de pouvoir trancher.\n\n    L'IC sur la pente vaut 1.96*sigma/sqrt(Sxx), et pour n pesees quotidiennes\n    couvrant n jours, Sxx = n(n^2-1)/12. On cherche le plus petit n tel que\n    l'IC devienne plus etroit que l'ecart a la consigne : c'est le moment ou\n    les donnees pourront enfin distinguer les deux.\n    \"\"\"\n    if sigma <= 0 or ecart_kg_sem <= 0:\n        return None\n    for n in range(max(n_actuel, 8), 400):\n        sxx = n * (n * n - 1) / 12.0\n        ic = 1.96 * sigma / (sxx ** 0.5) * 7.0\n        if ic < ecart_kg_sem:\n            return max(0, (n - n_actuel + 6) // 7)\n    return None\n\n\ndef analyser(payload):\n    d = json.loads(payload)\n    entries = d.get(\"entries\", {})\n    seances = d.get(\"seances\", [])\n    st = dict(E.PROFIL_DEFAUT)\n    st.update(d.get(\"settings\", {}))\n    fenetre = int(d.get(\"fenetre\", 28))\n    cible_pct = float(d.get(\"cible_pct\", 0.25))\n\n    out = {\"n_jours\": len(entries), \"n_seances\": len(seances), \"settings\": st}\n\n    ds, vs = _series(entries, \"poids\")\n    if len(vs) < 4:\n        out[\"erreur\"] = \"Moins de 4 pesees : rien a estimer.\"\n        return json.dumps(_propre(out), default=str, allow_nan=False)\n\n    lis = S.rolling_mean(vs, 7)\n    out[\"poids\"] = {\"dates\": [x.isoformat() for x in ds], \"brut\": vs, \"lisse\": lis}\n\n    # --- Regression sur les pesees BRUTES (jamais sur la moyenne glissante) --\n    cut = ds[-1] - timedelta(days=fenetre)\n    idx = [i for i, x in enumerate(ds) if x >= cut]\n    if len(idx) < 4:\n        idx = list(range(len(ds)))\n    xs = [float((ds[i] - ds[0]).days) for i in idx]\n    ys = [vs[i] for i in idx]\n    pente, ordo = S.polyfit1(xs, ys)\n    resid = [ys[i] - (pente * xs[i] + ordo) for i in range(len(xs))]\n    n = len(xs)\n    mx = S.mean(xs)\n    sxx = sum((v - mx) ** 2 for v in xs)\n    ss_res = sum(r * r for r in resid)\n    my = S.mean(ys)\n    sst = sum((v - my) ** 2 for v in ys)\n    r2 = 1 - ss_res / sst if sst > 0 else float(\"nan\")\n    sigma = S.std(resid, ddof=1) if n > 2 else 0.0\n    se = (ss_res / (n - 2) / sxx) ** 0.5 if sxx > 0 and n > 2 else 0.0\n\n    actuel = lis[-1]\n    pente_sem = pente * 7.0\n    ic = 1.96 * se * 7.0\n    out[\"tendance\"] = {\n        \"actuel\": actuel, \"pente_sem\": pente_sem, \"ic\": ic,\n        \"bas\": pente_sem - ic, \"haut\": pente_sem + ic,\n        \"pct_sem\": pente_sem / actuel * 100.0, \"r2\": r2, \"sigma\": sigma,\n        \"n\": n, \"fit\": {\"x0\": ds[idx[0]].isoformat(), \"x1\": ds[idx[-1]].isoformat(),\n                        \"y0\": pente * xs[0] + ordo, \"y1\": pente * xs[-1] + ordo},\n    }\n\n    # --- VERDICT : la consigne est-elle dans la bande de tolerance ? --------\n    cible = actuel * cible_pct / 100.0\n    if cible <= pente_sem - ic:\n        etat, msg = \"alarme\", \"Trop rapide. La consigne est SOUS la bande mesuree.\"\n    elif cible >= pente_sem + ic:\n        etat, msg = \"alarme\", \"Trop lent. La consigne est AU-DESSUS de la bande.\"\n    else:\n        etat, msg = \"nominal\", (\"Consigne dans la bande de tolerance. \"\n                                \"Ne change rien : tes donnees ne savent pas \"\n                                \"distinguer ta consigne de ce que tu observes.\")\n    ecart = abs(pente_sem - cible)\n    sem_req = _semaines_requises(sigma, ecart, n) if etat == \"nominal\" else None\n    out[\"verdict\"] = {\"etat\": etat, \"message\": msg, \"cible\": cible,\n                      \"cible_pct\": cible_pct, \"ecart\": ecart,\n                      \"semaines_requises\": sem_req}\n\n    # --- Eau vs tissu -------------------------------------------------------\n    if len(vs) >= 10:\n        der = S.mean(vs[-4:]) - S.mean(vs[-11:-4])\n        if abs(der) > 1.0:\n            out[\"alerte_eau\"] = {\"delta\": der, \"kcal\": abs(der) * E.KCAL_PAR_KG}\n\n    # --- Energie ------------------------------------------------------------\n    jours = _jours(entries, fenetre)\n    kcal = [j[\"entry\"][\"calories\"] for j in jours\n            if isinstance(j[\"entry\"].get(\"calories\"), (int, float))]\n    dp = [j[\"date\"] for j in jours if j.get(\"poids_brut\") is not None]\n    pp = [j[\"poids_brut\"] for j in jours if j.get(\"poids_brut\") is not None]\n    if len(pp) >= 4 and kcal:\n        ref = E.tdee_bilan(dp, pp, kcal)\n        if ref:\n            out[\"tdee\"] = ref\n            cal = E.calibrer(jours, st)\n            if cal:\n                ok, msg_k = E.plausibilite_k(cal[\"k\"])\n                out[\"calib\"] = {\"k\": cal[\"k\"], \"modele\": cal[\"modele_moyen\"],\n                                \"plausible\": ok, \"message\": msg_k,\n                                \"diagnostic\": E.diagnostic_k(cal[\"k\"])}\n            out[\"cibles\"] = [{\"pct\": p, \"kg_sem\": actuel * p / 100.0,\n                              \"kcal\": E.kcal_cible(ref[\"tdee\"], actuel, p)}\n                             for p in (0.20, 0.35, 0.50)]\n        if jours:\n            c = E.composantes(jours[-1][\"entry\"], jours[-1][\"poids_lisse\"], st)\n            out[\"composantes\"] = c\n\n    # --- Performances -------------------------------------------------------\n    if seances:\n        ex = P.par_exercice(seances)\n        tend = []\n        for cle, pts in ex.items():\n            t, nr = P.tendance_propre(pts)\n            if t:\n                tend.append({\"seance\": cle[0], \"exercice\": cle[1],\n                             \"pct_sem\": t[\"pct_sem\"], \"delta_pct\": t[\"delta_pct\"],\n                             \"actuel\": t[\"actuel\"], \"depart\": t[\"depart\"],\n                             \"n\": t[\"n\"], \"r2\": t[\"r2\"], \"ruptures\": nr,\n                             \"fiabilite\": P.fiabilite_e1rm(pts[-1][\"reps_max\"])})\n        tend.sort(key=lambda z: -z[\"pct_sem\"])\n        out[\"tendances\"] = tend\n        out[\"ruptures\"] = [\n            {\"seance\": cle[0], \"exercice\": cle[1], \"semaine\": r[\"semaine\"],\n             \"avant\": r[\"avant\"], \"apres\": r[\"apres\"],\n             \"baisse\": r[\"baisse_pct\"], \"notes\": r[\"notes\"]}\n            for cle, pts in ex.items() for r in P.detecter_ruptures(pts)]\n        ind, ecartes = P.indice_force(seances)\n        out[\"indice_force\"] = {str(k): v for k, v in ind.items()}\n\n        pw = {}\n        for s in seances:\n            dd = date.fromisoformat(s[\"date\"])\n            m = lundi(dd)\n            wv = [float(entries[(m + timedelta(days=i)).isoformat()][\"poids\"])\n                  for i in range(7)\n                  if (m + timedelta(days=i)).isoformat() in entries\n                  and \"poids\" in entries[(m + timedelta(days=i)).isoformat()]]\n            if wv:\n                pw[s[\"semaine\"]] = S.mean(wv)\n        cor = P.correlation_force_masse(seances, pw)\n        cor[\"diagnostic\"] = P.diagnostic_correlation(cor)\n        cor.pop(\"ecartes\", None)\n        out[\"correlation\"] = cor\n        out[\"poids_semaine\"] = {str(k): v for k, v in pw.items()}\n\n    return json.dumps(_propre(out), default=str, allow_nan=False)\n",
  }
};
